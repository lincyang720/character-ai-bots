{
  "name": "character-ai-bots",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build-index.js && node build-roleplay-page.js && node build-submit-page.js && node build-status-page.js && node generate-pages.js && node build-types.js && node build-blog.js && node build-quiz.js && node build-sitemap.js && node apply-site-config.js",
    "update": "node update-trending.js && npm run build"
  }
}
